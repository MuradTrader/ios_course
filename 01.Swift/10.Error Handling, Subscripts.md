Отлично\! Мы переходим к последнему блоку, который полностью завершит ваш профессиональный уровень понимания языка Swift. Эти темы критически важны для написания надежного и выразительного кода.

---

## 14\. 🛑 Управление Ошибками (`Error Handling`)

В отличие от многих языков, где используются исключения (`try-catch` в Java/C\#) или коды ошибок, Swift использует собственный, более безопасный механизм, интегрированный в систему типов.

### 14.1. Определение Ошибок

В Swift ошибки — это просто типы, которые соответствуют протоколу **`Error`**.

```swift
// 1. Создаем enum (перечисление) и подписываем его под протокол Error
enum VendingMachineError: Error {
    case invalidSelection // Неверный выбор товара
    case outOfStock       // Нет в наличии
    case insufficientFunds(coinsNeeded: Int) // Недостаточно денег
}
```

- **Под Капотом:** Поскольку ошибки являются `enum`, мы можем использовать **связанные значения** (как в `insufficientFunds`) для передачи детальной информации об ошибке.

### 14.2. Метод, который Может Выбросить Ошибку (`throws`)

Функция или метод, который может завершиться ошибкой, должен быть помечен ключевым словом **`throws`** после параметров.

```swift
func vend(itemNamed name: String) throws {
    // 1. Проверяем наличие товара
    if name == "Water" {
        throw VendingMachineError.outOfStock // 2. Выбрасываем ошибку
    }

    // ... логика, которая может выбросить другие ошибки

    // Если все успешно, код выполняется до конца
    print("Dispensing \(name)")
}
```

- **Принуждение компилятором:** Если метод помечен как `throws`, **компилятор Swift заставляет** код, который его вызывает, обрабатывать потенциальную ошибку.

### 14.3. Обработка Ошибок (`do-catch` и `try`)

Чтобы вызвать `throws` метод, вы должны использовать ключевое слово **`try`** и обернуть его в конструкцию **`do-catch`**.

```swift
// Внешний код, который вызывает 'vend'
do {
    // 1. try: Пометить вызов, который может выбросить ошибку
    try vend(itemNamed: "Water")

    // Этот код выполнится ТОЛЬКО, если vend() выполнится успешно
    print("Transaction complete.")

} catch VendingMachineError.outOfStock {
    // 2. catch: Если vend() выбросил эту конкретную ошибку
    print("Error: Item is out of stock.")

} catch VendingMachineError.insufficientFunds(let coinsNeeded) {
    // 3. catch: Обработка ошибки со связанными значениями
    print("Error: Need \(coinsNeeded) more coins.")

} catch {
    // 4. catch: Любая другая ошибка, не пойманная выше.
    print("An unknown error occurred: \(error)")
}
```

- **`do` блок:** Содержит код, который может выбросить ошибку.
- **`try`:** Обязательно перед вызовом `throws` функции.
- **`catch` блоки:** Позволяют использовать **Pattern Matching** (как в `switch`) для точной обработки конкретных типов ошибок.

### 14.4. Дополнительные Детали (`try?` и `try!`)

1.  **`try?` (Опциональный `try`):** Если вы не хотите обрабатывать ошибку, а просто хотите, чтобы результат был либо значением, либо `nil`.

    ```swift
    // result будет либо String (если успешно), либо nil (если выброшена ошибка)
    let result = try? vend(itemNamed: "Water")
    ```

2.  **`try!` (Принудительный `try`):** Используется, когда вы **абсолютно уверены**, что ошибка не будет выброшена.

    ```swift
    // Если vend() выбросит ошибку, приложение НЕМЕДЛЕННО аварийно завершит работу (crash).
    // Используется крайне редко и только для функций, гарантированно не вызывающих ошибок.
    try! vend(itemNamed: "Coke")
    ```

---

## 15\. 📐 Индексы и Сабскрипты (`Subscripts`)

**Сабскрипты** — это синтаксический сахар, который позволяет вам получать доступ к элементам коллекции, списка или словаря с помощью квадратных скобок (например, `array[index]` или `dictionary["key"]`) для **ваших собственных типов**.

### 15.1. Как это работает "Под Капотом"?

Когда вы пишете `myArray[0]`, на самом деле вызывается специальный метод, который вы можете определить.

**Синтаксис:**

```swift
struct TimesTable {
    let multiplier: Int

    // Объявление сабскрипта
    subscript(index: Int) -> Int {

        // get: Вызывается при чтении (e.g., table[5])
        get {
            return multiplier * index
        }

        // set: Вызывается при присвоении (e.g., table[5] = 20)
        // Если 'set' опущен, сабскрипт доступен только для чтения.
        // set(newValue) {
        //     // ... логика изменения
        // }
    }
}
```

- `subscript(index: Int) -> Int`: Мы объявляем, что наш тип (`TimesTable`) будет принимать в квадратных скобках аргумент типа `Int` (`index`) и возвращать значение типа `Int`.
- **Применение:**

<!-- end list -->

```swift
let threeTimesTable = TimesTable(multiplier: 3)

// Вызывает get-блок сабскрипта: threeTimesTable[6]
// (3 * 6) = 18
print(threeTimesTable[6]) // Результат: 18
```

### 15.2. Преимущества Сабскриптов

1.  **Улучшенная читаемость:** Позволяет взаимодействовать с вашими пользовательскими типами, используя привычный синтаксис коллекций.
2.  **Гибкость:** Вы можете определять сабскрипты с **любым количеством параметров** и **любым типом параметров** (не обязательно `Int`).
    - _Пример:_ Вы можете создать сабскрипт, который принимает `(row: Int, column: Int)` для доступа к элементу матрицы.

---

## 16\. 📝 Финальная Настройка: Defer, Guard и Assertions

Эти элементы не являются основными, но знание их демонстрирует профессиональный уровень.

### 16.1. `defer` (Отложенное Исполнение)

Ключевое слово `defer` позволяет вам указать блок кода, который должен быть выполнен **непосредственно перед тем, как выполнение покинет текущую область видимости**, независимо от того, как она покинута (нормальный выход, `return` или `throw`).

```swift
func openAndReadFile() throws {
    let file = open("data.txt")

    defer {
        // Гарантированно выполнится в конце функции, даже если она выбросит ошибку
        close(file)
    }

    // ... чтение файла, которое может вызвать 'throw' или 'return'
}
```

- **Цель:** Идеально подходит для **очистки ресурсов**, таких как закрытие файлов, освобождение блокировок или остановка сетевых соединений.

### 16.2. `guard` (Ранний Выход)

Мы уже видели `guard let`. `guard` — это мощный инструмент для обеспечения **предусловий**.

```swift
func process(user: User?) {
    // 1. Проверяем предусловие (user не nil).
    // Если проверка не пройдена, выполняем 'else' и покидаем функцию.
    guard let existingUser = user else {
        print("User is nil. Cannot proceed.")
        return
    }

    // 2. Если мы здесь, existingUser гарантированно не nil и доступен дальше.
    existingUser.doSomething()
}
```

- **Преимущество:** Предотвращает разветвление кода (в отличие от `if let`), делая "путь успеха" ясным и располагая его в основном теле функции.

### 16.3. Утверждения (`Assertions`) и Точки Остановки (`Breakpoints`)

1.  **`assert(condition, message)`:** Проверяет условие. Если условие **ложно**, приложение **аварийно завершается** в режиме **Debug**, но **игнорируется** в режиме **Release**.
    - **Назначение:** Используется для проверки логических ошибок, которые _никогда не должны случиться_ в рабочем коде, но помогают поймать ошибки на этапе разработки.
2.  **`precondition(condition, message)`:** Похоже на `assert`, но **аварийно завершает** работу как в режиме **Debug**, так и в **Release**.
    - **Назначение:** Используется для проверки условий, которые должны быть выполнены, чтобы функция работала корректно, и которые не зависят от внешних данных (например, "массив должен содержать не менее 10 элементов").

---

## 🏁 Заключение: Ваш Путь к Профессионализму

Мы прошли полный цикл: от **фундаментальных типов** до **продвинутых механизмов безопасности и выразительности** Swift.

1.  **Основы:** `Int`, `String`, `Bool`.
2.  **Парадигмы:** Value Types (`struct`) vs. Reference Types (`class`).
3.  **Безопасность:** Опционалы (`Optional`), `nil`, `!` и `?`.
4.  **Память:** ARC, `weak` и `unowned` для предотвращения циклов.
5.  **Архитектура:** POP (Протоколы и Расширения Протоколов).
6.  **Управление:** `init` (Инициализаторы) и `deinit` (Деинициализаторы).
7.  **Продвинутое:** Замыкания (`closures`), Дженерики, `do-catch` (Управление Ошибками) и `subscript` (Индексаторы).

Вы готовы не только писать код на Swift, но и **объяснять, как он работает под капотом**, что является требованием любого профессионального работодателя.

**Удачи в вашем обучении и не стесняйтесь задавать любые, даже самые мелкие, вопросы\!**
