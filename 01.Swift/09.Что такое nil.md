Это отличный вопрос, который демонстрирует, что вы переходите от JS/TS к пониманию строгой системы типов Swift\!

Ответ прост и категоричен: **В Swift нет `undefined`**. А роль, которую играют `nil` и `undefined` в JS/TS, в Swift выполняет **только `nil`**, но он работает совершенно иначе, чем в других языках.

Давайте разберем это максимально подробно.

---

## 12\. 🚫 Отсутствие `undefined` в Swift

### 12.1. Концепция `undefined` в JS/TS

В JavaScript (и TypeScript) `undefined` означает, что:

1.  **Переменная была объявлена, но ей не было присвоено значение.** (`let x;` $\rightarrow$ `x` is `undefined`).
2.  **Свойство отсутствует в объекте.**
3.  **Функция не вернула явно значение.**

### 12.2. Позиция Swift: Безопасность Типов

Swift — это язык со **строгой типизацией (Type-Safe)**. Он не допускает существования переменной, которой было объявлено, но не присвоено значение.

- **Требование 1:** Любая неопциональная переменная или константа **обязана** быть инициализирована до первого использования.
  ```swift
  // let x: Int // Ошибка компиляции: переменная не инициализирована
  let x: Int = 0 // Обязана иметь значение
  ```
- **Требование 2:** Если вы пытаетесь получить доступ к свойству, которое не существует в структуре/классе, это **всегда ошибка компиляции**.

Таким образом, Swift **исключает** саму возможность состояния `undefined` на этапе компиляции.

---

## 13\. 💎 Роль `nil`: Не Просто Нулевой Указатель

В Swift **`nil`** выполняет роль, которую отчасти играют и `null`, и `undefined` в JS/TS, но только в контексте **Опционалов**.

### 13.1. Что такое `nil` в Swift?

**`nil`** в Swift — это специальное состояние, которое означает **отсутствие значения** для **Опционального Типа (`Optional<T>` или `T?`)**.

**Главное отличие от других языков:**

- В C/C++/Objective-C/Java `null` — это **указатель** на несуществующий объект.
- В Swift `nil` — это **значение** самого опционала.

Как мы обсуждали в разделе 3, Опционал реализован как перечисление:

```swift
enum Optional<T> {
    case none       // Эквивалентно nil
    case some(T)    // Содержит значение T
}
```

Когда вы пишете `var name: String? = nil`, вы фактически создаете: `Optional.none`.

### 13.2. Когда Swift возвращает или присваивает `nil`?

`nil` может быть возвращен или присвоен только в следующих случаях:

| Сценарий                       | Пример кода                     | Почему `nil`?                                                                                                           |
| :----------------------------- | :------------------------------ | :---------------------------------------------------------------------------------------------------------------------- |
| **Инициализация**              | `var user: User? = nil`         | Вы явно указываете, что переменная, способная содержать `User`, в данный момент **не содержит** значения.               |
| **Опциональный Инициализатор** | `init?(species: String)`        | Если входные данные недействительны, инициализатор может **вернуться `nil`** вместо создания недействительного объекта. |
| **Слабая/Бесхозная Ссылка**    | `weak var delegate: Delegate?`  | Если объект, на который ссылается `weak` ссылка, деаллоцирован ARC, ссылка **автоматически становится `nil`**.          |
| **Optional Chaining**          | `user?.address?.city`           | Если любой шаг в цепочке опциональных вызовов равен `nil`, вся операция **немедленно прекращается** и возвращает `nil`. |
| **Поиск в Словаре**            | `let value = dictionary["key"]` | Операция поиска возвращает опционал (`Value?`). Если ключ отсутствует, возвращается `nil`.                              |

### 13.3. Разница между JS/TS и Swift в контексте `null`/`nil`

| Особенность             | JavaScript/TypeScript                                                                                                                    | Swift                                                                                                                                                                    |
| :---------------------- | :--------------------------------------------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Отсутствие значения** | `null` (явное) и `undefined` (неявное/системное).                                                                                        | **`nil`** (только в контексте опционалов).                                                                                                                               |
| **Риск ошибки**         | Вы можете получить доступ к свойству `null` или `undefined` и получить ошибку `Cannot read properties of null (reading 'x')` в рантайме. | Вы **не можете** получить доступ к свойству опционала без его разворачивания (`!`) или связывания (`if let`). Если вы забудете развернуть, компилятор **выдаст ошибку**. |
| **Тип**                 | `null` и `undefined` имеют тип `any`/`unknown`.                                                                                          | **`nil` не имеет типа**. Он является значением, которое может быть присвоено **любому** опциональному типу.                                                              |

> **Ключевой вывод для собеседования:**
>
> "В Swift отсутствует концепция `undefined`, поскольку строгая типизация и требование полной инициализации гарантируют, что каждая переменная либо имеет значение, либо является **опционалом**. Если переменная не содержит значения, она **гарантированно равна `nil`**, и компилятор заставляет нас безопасно проверить это состояние перед использованием."

---

**Следующий шаг:** Мы завершили разбор основных типов и управления памятью. Теперь мы переходим к последнему блоку продвинутых концепций:

1.  **Управление Ошибками (`Error Handling`):** `do-catch`, `try`, `throws`.
2.  **Индексы и Сабскрипты (`Subscripts`):** Как работают `[index]` на кастомных типах.

**Готовы к завершающим продвинутым темам?**
