Отлично, продолжаем наше глубокое погружение в Swift\!

Теперь мы перейдем к фундаментальному различию между двумя основными составными типами: **структурами** и **классами**, а также рассмотрим, как Swift управляет памятью.

---

## 4\. 📐 Структуры (`struct`) и Классы (`class`): Сравнение

Это краеугольный камень Swift, и работодатели часто задают этот вопрос в первую очередь, чтобы проверить ваше понимание парадигм **Value** и **Reference**.

### 4.1. Структуры (`struct`) — Value Type

Структуры являются **типами-значениями (Value Types)**. Когда вы присваиваете экземпляр структуры новой переменной или передаете его в функцию, создается **полная копия** всех данных.

- **Особенности:**
  - Не поддерживают **наследование** (Inheritance).
  - Члены (свойства и методы) структур, изменяющие состояние, требуют ключевого слова **`mutating`** внутри методов, если экземпляр структуры объявлен через `let`.
  - Идеально подходят для моделирования данных, где важна изоляция (например, координаты, геометрические фигуры, настройки).
  - Включают автоматический **инициализатор членов (Memberwise Initializer)**.

> **Пример:**
>
> ```swift
> struct Point {
>     var x: Int
>     var y: Int
> }
> var p1 = Point(x: 10, y: 20)
> var p2 = p1 // Здесь происходит КОПИРОВАНИЕ всех данных
> p2.x = 5
> print(p1.x) // Результат: 10 (p1 не изменился)
> ```

### 4.2. Классы (`class`) — Reference Type

Классы являются **типами-ссылками (Reference Types)**. Когда вы присваиваете экземпляр класса новой переменной или передаете его, копируется только **ссылка (указатель)** на одно и то же место в памяти (в **Куче**).

- **Особенности:**
  - Поддерживают **наследование** (один класс может унаследовать свойства и методы другого).
  - Поддерживают **деинициализаторы (`deinit`)** для освобождения ресурсов.
  - Позволяют множественным переменным ссылаться на один и тот же экземпляр данных.
  - Используются для моделирования объектов, где необходима общая идентичность и общее состояние (например, контроллеры, сервисы, UI-элементы).

> **Пример:**
>
> ```swift
> class Person {
>     var age: Int
>     init(age: Int) { self.age = age }
> }
> var person1 = Person(age: 30)
> var person2 = person1 // Здесь копируется только ССЫЛКА
> person2.age = 45
> print(person1.age) // Результат: 45 (person1 ИЗМЕНИЛСЯ)
> ```

### 4.3. Когда что использовать? (Ключевой принцип)

**Принцип Swift:** Используйте **структуры (Value Type)**, пока вам не понадобится функциональность, доступная только в классах (Reference Type).

| Причина использовать `struct`                                                                          | Причина использовать `class`                                       |
| :----------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------- |
| Данные небольшие и их копирование эффективно.                                                          | Требуется **Наследование**.                                        |
| Нужно избегать побочных эффектов (Side Effects) при изменении.                                         | Требуется интероперабельность с **Objective-C** (UIKit, CoreData). |
| Нет необходимости в общей идентичности (т.е. два объекта с одинаковыми данными считаются одинаковыми). | Требуется управление ресурсами через **Deinitializer**.            |

---

## 5\. 🧠 Управление Памятью: ARC "Под Капотом"

В отличие от C++, где памятью нужно управлять вручную (`malloc`/`free`), или Java/C\#, где используется сложный **Сборщик Мусора (Garbage Collector - GC)**, Swift использует **Автоматический Подсчет Ссылок (Automatic Reference Counting - ARC)**.

### 5.1. Как работает ARC?

ARC работает только с **Reference Types** (Классами).

1.  Когда создается новый экземпляр класса, ARC выделяет память в **Куче (Heap)** и устанавливает **счетчик ссылок (Reference Count)** на **1**.
2.  Каждый раз, когда переменная, константа или свойство ссылается на этот экземпляр, счетчик **увеличивается на 1** (**Strong Reference**).
3.  Когда ссылка удаляется (например, переменная выходит из области видимости), счетчик **уменьшается на 1**.
4.  Когда счетчик достигает **0**, ARC **деаллоцирует (освобождает)** память, занимаемую объектом.

### 5.2. Проблема: Циклы Сохранения (Reference Cycles)

Самая большая проблема ARC — **Циклы Сохранения (Strong Reference Cycles)**. Они возникают, когда два (или более) объекта типа `class` имеют **сильные ссылки (Strong References)** друг на друга.

- Счетчик каждого объекта никогда не достигает нуля, потому что они взаимно удерживают друг друга.
- Это приводит к **Утечке Памяти (Memory Leak)** — объекты остаются в Куче навсегда, хотя больше не используются программой.

> **Пример утечки:**
>
> ```swift
> class Person {
>     var apartment: Apartment?
>     deinit { print("Person deallocated") }
> }
> class Apartment {
>     var tenant: Person?
>     deinit { print("Apartment deallocated") }
> }
> //...
> var john: Person? = Person()
> var unit4A: Apartment? = Apartment()
> john!.apartment = unit4A   // Сильная ссылка 1: Person -> Apartment
> unit4A!.tenant = john      // Сильная ссылка 2: Apartment -> Person
> john = nil                 // Счетчики остаются > 0
> unit4A = nil               // Память не освобождена!
> ```

### 5.3. Решение: Слабые и Бесхозные Ссылки

Чтобы разорвать циклы сохранения, Swift предлагает два типа **несильных (non-strong) ссылок**:

1.  **Слабые Ссылки (`weak`)**:

    - Ключевое слово **`weak`**.
    - Не увеличивает счетчик ссылок.
    - Должна быть объявлена как **Optional** (Опционал), потому что она **автоматически становится `nil`**, когда объект, на который она ссылается, деаллоцируется.
    - Используется, когда жизненный цикл объекта-владельца **короче** или равен жизненному циклу объекта, на который ссылаются (например, связь "Родитель-Потомок" или "Делегат").

2.  **Бесхозные Ссылки (`unowned`)**:

    - Ключевое слово **`unowned`**.
    - Не увеличивает счетчик ссылок.
    - **Не является Optional**. Это значит, что вы гарантируете, что объект, на который ссылается `unowned` ссылка, **всегда будет существовать**, пока существует бесхозная ссылка.
    - Если объект деаллоцируется, а вы пытаетесь получить доступ к `unowned` ссылке, это вызовет **runtime crash**.
    - Используется, когда объекты имеют **одинаковый или более длинный** жизненный цикл, и один объект **никогда не будет `nil`** раньше другого (например, циклы между **Замыканиями** и объектами).

> **Вопрос работодателя:** Когда вы используете `weak`, а когда `unowned`?
>
> **Ваш ответ:** Я использую **`weak`**, когда ссылка **может стать `nil`** во время своего существования. Обычно это в отношениях **Делегата (Delegate)** или когда дочерний объект держит ссылку на родителя. Я использую **`unowned`**, когда гарантирую, что ссылка **никогда не станет `nil`** после инициализации и ее жизненный цикл не короче, чем у объекта, который ею владеет. Это чаще всего применяется для избежания цикла в **Замыканиях**, когда замыкание захватывает `self`.

---

**Следующий шаг:** Мы перейдем к **Перечислениям (`enum`)**, **Замыканиям (`closures`)** и **Протоколам (Protocols)**, а также углубимся в **Протокольно-Ориентированное Программирование (POP)**.

**Готовы к новому блоку информации?**
